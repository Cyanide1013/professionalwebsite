var name= prompt("enter ur name:");
var firstLetter= name.slice(0,1);
var remaingLetter= name.slice(1,name.length);
var firstLetterUpper=firstLetter.toUpperCase();
alert("hello "+ firstLetterUpper + remaingLetter);
